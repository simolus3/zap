`zap_dev` is the development package for `zap` projects.

At the moment, it contains a builder to transform `.zap` files into generated
Dart components.

For more information, see https://simonbinder.eu/zap/.
