name: showcase
publish_to: none

environment:
  sdk: '>=2.15.1 <3.0.0'

dependencies:
  js: ^0.6.3
  rxdart:
  zap:

dev_dependencies:
  build_runner: ^2.1.7
  build_web_compilers: ^3.0.0
  webdev: ^2.7.7
  zap_dev:

dependency_overrides:
  zap:
    path: ../zap
  zap_dev:
    path: ../zap_dev
