name: zap_docs
publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.2

dependencies:
  picocss:
    version: ^1.5.10
    hosted: https://simonbinder.eu
  tar: ^2.0.0
  riverpod_zap: ^0.2.0
    # version: ^0.1.1
    # hosted: https://simonbinder.eu
  web: ^1.1.1
  zap: ^0.3.0

dev_dependencies:
  build: ^3.0.0
  build_runner: ^2.6.0
  build_web_compilers: ^4.2.0
  pubspec_parse: ^1.5.0
  zap_dev: ^0.3.0
  # built_site:
  #   version: ^0.2.9
  #   hosted: https://simonbinder.eu
